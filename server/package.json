{
  "name": "docker",
  "version": "1.0.0",
  "description": "1. Command: `docker build`\r    Description: Build a Docker image from a Dockerfile.\r    Syntax: \r    ``` \r    docker build -t <image_name> [options] <path_to_Dockerfile>\r    ```\r    Explanation:\r    - `-t`: Tags the image with a name and optional tag in the `name:tag` format.\r    - `<image_name>`: Specifies the name you want to give to the image.\r    - `[options]`: Additional options that can be used during the build process (e.g., `--no-cache` to ignore cached layers).\r    - `<path_to_Dockerfile>`: The path to the directory containing the Dockerfile.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./build/app.js",
    "build": "tsc",
    "dev": "nodemon ./src/app.ts",
    "dev:build": "ts-node ./src/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/mongoose": "^5.11.97",
    "@types/morgan": "^1.9.4",
    "@types/node": "^20.4.5",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "mongoose": "^7.4.1",
    "morgan": "^1.10.0"
  }
}
